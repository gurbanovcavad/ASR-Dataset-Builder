class Validator:
    def validate(self, path: str) -> bool:
        # TODO - implement validator (probe with ffprobe, check audio duration, check if the parameters of the audio match the ones in the config)
        return True